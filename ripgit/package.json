{
  "name": "ripgit-tests",
  "version": "0.1.4",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:worker": "bash ../scripts/worker-build.sh ripgit --release",
    "test": "npm run build:worker && NODE_OPTIONS='--experimental-vm-modules' vitest run"
  },
  "devDependencies": {
    "miniflare": "4.20260317.1",
    "vitest": "3.2.4",
    "wrangler": "^4.83.0"
  }
}
