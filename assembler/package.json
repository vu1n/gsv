{
  "name": "assembler",
  "version": "0.1.4",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:worker": "bash ../scripts/worker-build.sh assembler --release",
    "test:rust": "cargo test",
    "test": "cargo test"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
